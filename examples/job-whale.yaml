---
apiVersion: batch/v1
kind: Job
metadata:
  name: job-sleep-whale
  namespace: test-slurm
  # slurm-bridge annotations on parent object
  annotations:
    slinky.slurm.net/job-name: job-sleep-whale
    slurmjob.slinky.slurm.net/account: slurm
    slurmjob.slinky.slurm.net/partition: all
    slurmjob.slinky.slurm.net/timelimit: "60"
spec:
  completions: 3
  parallelism: 3
  template:
    spec:
      containers:
        - name: sleep
          image: quay.io/prometheus/busybox
          command: [sh, -c, sleep 30000]
          resources:
            requests:
              cpu: '1'
              memory: 100Mi
            limits:
              cpu: '1'
              memory: 100Mi
      restartPolicy: Never
